name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install core dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy pandas scikit-learn lightgbm pyarrow

      - name: Install dependencies
        run: |
          # Install key packages via pip (they should use pre-built wheels)
          pip install yfinance mlforecast statsforecast optuna
          
          # Skip u8darts[torch] in CI - too heavy, requires scipy compilation
          # Instead test the lighter imports
          echo "Installed core packages successfully"

      - name: Lint (ruff)
        run: |
          pip install ruff
          ruff check src/ || true  # Skip if no src/ yet
          ruff format --check src/ || true

      - name: Type check (pyright)
        run: |
          pip install pyright
          pyright src/ || true  # Skip if no src/ yet

      - name: Run smoke tests
        run: |
          python -c "
          import numpy
          import pandas
          import sklearn
          import lightgbm
          import pyarrow
          print('Core imports OK')
          print(f'numpy: {numpy.__version__}')
          print(f'pandas: {pandas.__version__}')
          print(f'scikit-learn: {sklearn.__version__}')
          print(f'lightgbm: {lightgbm.__version__}')
          "

  validate-env:
    name: Validate Environment Consistency
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check requirements.txt validity
        run: |
          python -c "
          with open('requirements.txt') as f:
              lines = [l.strip() for l in f if l.strip() and not l.startswith('#')]
          print(f'{len(lines)} requirements found')
          assert len(lines) > 0, 'requirements.txt is empty'
          "

      - name: Validate environment.yml (if exists)
        run: |
          test -f environment.yml && echo "environment.yml present" || echo "No environment.yml (using conda export)"

      - name: Validate direnv setup
        run: |
          test -f .envrc && echo "direnv .envrc present" || echo "No .envrc found"
